---
name: sapid-deploy
description: Commands and instructions for deploying the Flutter app to production.
---

## Android

To release the Flutter app on Android, run the following Fastlane command:

```bash
cd /android && fastlane internal
```

## iOS

To release the Flutter app on Android, run the following Fastlane command:

```bash
cd /ios && fastlane internal
```

## Web