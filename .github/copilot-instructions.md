Use Signal for State management.
Use auto_route for routing (app/router.dart). 
Use get_it and injectable for registering services (app/services.dart).
Use constant gaps instead of SizedBox (ex. gap16).
Don't use underscores in function or variable names.